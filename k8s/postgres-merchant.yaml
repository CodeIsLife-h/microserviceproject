apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-merchant-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-merchant
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-merchant
  template:
    metadata:
      labels:
        app: postgres-merchant
    spec:
      containers:
        - name: postgres-merchant
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: merchantdb
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: password
          volumeMounts:
            - name: merchant-data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
      volumes:
        - name: merchant-data
          persistentVolumeClaim:
            claimName: postgres-merchant-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-merchant
spec:
  selector:
    app: postgres-merchant
  ports:
    - port: 5432
      targetPort: 5432
